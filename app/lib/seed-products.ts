import type { Product } from "../product-data";

/**
 * Optional sample catalog for local demos.
 *
 * The live shop is filled from admin (Cloudinary uploads). This list stays
 * empty on purpose so a fresh deploy never ships placeholder Nike photography.
 * `npm run seed` is a no-op until you add entries here for your own fixtures.
 */
export const SEED_PRODUCTS: Product[] = [];
